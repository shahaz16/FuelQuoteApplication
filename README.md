# FuelQuoteApplication

to run frontend type this command:
  ->cd client
  ->npm install
  ->npm start

to run backend type this command:
  ->cd server
  ->npm install
  ->npm start

to run tests:
 ->npm test
